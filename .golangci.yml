version: "2"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - gosec
    - nolintlint

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - gosec
